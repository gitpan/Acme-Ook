#!/usr/bin/perl -ws

use strict;
use vars qw($S $l);

use Acme::Ook;
my $Ook = Acme::Ook->new;
my $OOK = $Ook->compile(@ARGV);

if ($S) {
    print $OOK;
} else {
    eval $OOK;
}

print "\n" if $l;

exit(0);



